[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions.environment]
  NODE_OPTIONS = "--experimental-fetch"

[context.production.environment]
  METRO_API_KEY = "<your key>"